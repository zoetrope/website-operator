apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui
  namespace: system
spec:
  template:
    spec:
      containers:
        - name: ui
          image: ghcr.io/cybozu-go/website-operator-ui:dev-amd64
